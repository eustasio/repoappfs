{
  "name": "repoappfs",
  "version": "1.0.0",
  "description": "création d'une application de consultation de repo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "commit": "bash ./npm-run-commit.sh",
    "dev": "concurrently \"npm --prefix client run dev\" \"npm --prefix server run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2",
    "currently": "^0.0.8"
  }
}
